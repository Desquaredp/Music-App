package edu.usfca.cs;

/**
 * this is an interface that is used in the Library class and contains the structure for a duplicates test
 * @author Deep Mistry
 **/
public interface dup {
    boolean duptes(Song sng);
}